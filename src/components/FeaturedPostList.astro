---
import { getCollection } from "astro:content";
import Post from "./Post.astro";
const posts = await getCollection("posts");
---

<div class="flex flex-col gap-3">
  {posts.reverse().map((post) => post.data.featured && <Post post={post} />)}
</div>
