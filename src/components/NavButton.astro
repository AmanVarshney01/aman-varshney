---
type Props = {
	name: string
	href: string
	isActive: boolean
}

const { name, href, isActive } = Astro.props
---

<a href={href} rel="noopener noreferrer" class={isActive ? '' : 'no-underline'}>
	{name}
</a>
