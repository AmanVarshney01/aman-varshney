---
type Props = {
	name: string
	href: string
	isActive: boolean
}

const { name, href, isActive } = Astro.props
---

<a
	href={href}
	rel="noopener noreferrer"
	class={`text-sm ${isActive ? 'text-accent font-medium' : 'text-muted hover:text-foreground'}`}
>
	{name}
</a>
