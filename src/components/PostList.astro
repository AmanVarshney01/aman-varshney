---
import { getCollection } from "astro:content";
import Post from "./Post.astro";
const posts = await getCollection("posts");
---

<div class="flex flex-col gap-4">
  {posts.reverse().map((post) => <Post post={post} />)}
</div>
