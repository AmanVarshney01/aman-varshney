import { db, Guestbook } from "astro:db";

// https://astro.build/db/seed
export default async function seed() {
  // await db.insert(Guestbook).values([
  //   { name: "Astro", message: "Hello, world!" },
  //   { name: "Astro", message: "Hello, world!" },
  //   { name: "Astro", message: "Hello, world!" },
  // ]);
}
